package rbp.registry.block.cakeconstructionstages;

import net.minecraft.block.Block;

public class DestructibleBlock extends Block{
    public DestructibleBlock(Settings settings) {
        super(settings);
    }
}
